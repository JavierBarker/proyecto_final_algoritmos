CLASE Utilidades
    // Métodos Públicos
    PUBLICO
        FUNCION LOGICO autenticar(usuario POR REFERENCIA, contrasena POR REFERENCIA)
        PROCEDIMIENTO limpiarPantalla()
        PROCEDIMIENTO limpiarPantallaValidar()
        FUNCION CADENA obtenerFechaActual()
        FUNCION REAL calcularMulta() // Declarado, pero la implementación está en Prestamo
FIN CLASE
